name: Lint

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  lint:
    uses: trailofbits/.github/.github/workflows/lint.yml@v0.0.3
    with:
      language: python
      python-version: "3.7"
